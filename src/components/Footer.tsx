import styled from 'styled-components';
import * as React from 'react';

const FooterDiv = styled.div`
  background-color: #bbb;
  text-align: center;
`;

export default () => {
  return <FooterDiv>@Tornado Table Tennis Club</FooterDiv>;
};
